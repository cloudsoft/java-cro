########################################################
# Hoodie Shop Frontend Service & Deployment
#######################################################
apiVersion: v1
kind: Service
metadata:
  name: hoodie-frontend
  labels:
    role: ui
    tier: frontend
    groupId: hoodie-shop
spec:
  selector:
    name: hoodie-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hoodie-frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      name: hoodie-frontend
  template:
    metadata:
      labels:
        name: hoodie-frontend
    spec:
      containers:
        - name: hoodie-frontend
          image: hoodie-frontend:1.0
          ports:
            - containerPort: 3000
              protocol: TCP
          env:
            - name: REACT_APP_BACKEND_URL
              value: http://localhost:30895
---
